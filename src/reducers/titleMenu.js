const style = {
    textAlign: "center"
};

const initState = {
    title: "seowon",
    zDepth: 2,
    iconClassNameRight: "muidocs-icon-navigation-expand-more",
    style: style
};

const titleMenu = (state = initState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default titleMenu;